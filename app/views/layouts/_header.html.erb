
<div class="container-fluid">
  <nav class="navbar navbar-default navbar-fixed-top" style="border-color:white;">
    <div class="navbar-header">
        <button type="button"
                class="navbar-toggle collapsed"
                data-toggle="collapse"
                data-target="#navbar"
                aria-expanded="false"
                aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li <%= 'class="active"' if @active == 'home'  %>><a href="/">home</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li><%= link_to "search", user_path(current_user) %></li>
          <li><%= link_to "edit profile", edit_user_path(current_user) %></li>
          <li><%= link_to "sign out", signout_path, method: 'delete' %></li>
        <% else %>
          <li><%= link_to "sign up", signup_path %></li>
          <li><%= link_to "sign in", signin_path %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </nav>
</div>
<div>
  <!-- <img src="/images/DRZ.png" width="50%" height="25%"> -->
</div>
<audio autoplay>
  <source src="/images/elevate.mp3" type="audio/mpeg" autoplay>
  Your browser does not support the audio element.
</audio>